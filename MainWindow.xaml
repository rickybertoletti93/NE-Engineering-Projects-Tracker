<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="NE UAE Project Tracker"
        Height="920"
        Width="1380"
        WindowStartupLocation="CenterScreen"
        Background="#0D1017"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="False"
        StateChanged="Window_StateChanged"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True" Icon="/NEUAEPT_Logo.png">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                        CornerRadius="0"
                        GlassFrameThickness="0"
                        ResizeBorderThickness="6"
                        UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>

        <SolidColorBrush x:Key="SidebarBorderBrush" Color="#2A2F38"/>
        <SolidColorBrush x:Key="TopbarBrush" Color="#10151B"/>
        <SolidColorBrush x:Key="TopbarSearchBrush" Color="#1A2028"/>
        <SolidColorBrush x:Key="WindowButtonBrush" Color="#202733"/>
        <SolidColorBrush x:Key="PrimaryRedBrush" Color="#D90429"/>
        <SolidColorBrush x:Key="SidebarTextBrush" Color="#D7DCE7"/>
        <SolidColorBrush x:Key="SidebarMutedTextBrush" Color="#98A2B3"/>
        <SolidColorBrush x:Key="BodyBackgroundBrush" Color="#10151B"/>

        <LinearGradientBrush x:Key="SidebarBackgroundGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#2E333B" Offset="0"/>
            <GradientStop Color="#22272F" Offset="0.5"/>
            <GradientStop Color="#1C2027" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="AccentGradientBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#E11134" Offset="0"/>
            <GradientStop Color="#A60321" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="52"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="14"
                                Padding="18,0">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="#242A39"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="Active">
                                <Setter TargetName="RootBorder" Property="Background" Value="{StaticResource AccentGradientBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowCommandButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="Background" Value="{StaticResource WindowButtonBrush}"/>
            <Setter Property="Foreground" Value="#E5E7EB"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2B3340"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowCommandButtonStyle}">
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Background" Value="#D90429"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2B3340"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SidebarIconTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="TopBarLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#B8C0CC"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="8,0,0,6"/>
        </Style>

        <Style x:Key="TopBarComboBoxToggleStyle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="Transparent">
                            <TextBlock Text="▼"
                               Foreground="#98A2B3"
                               FontSize="10"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TopBarComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#E5E7EB"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                        Background="{TemplateBinding Background}"
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#2A313C"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#D90429"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TopBarComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Foreground" Value="#E5E7EB"/>
            <Setter Property="Background" Value="{StaticResource TopbarSearchBrush}"/>
            <Setter Property="BorderBrush" Value="#313845"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="14,0,40,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource TopBarComboBoxItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="MainBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="14"/>

                            <ContentPresenter Margin="14,0,40,0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"
                                      Content="{TemplateBinding SelectionBoxItem}"
                                      ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"/>

                            <ToggleButton Grid.Column="0"
                                  Width="34"
                                  HorizontalAlignment="Right"
                                  Margin="0,0,8,0"
                                  IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                  Style="{StaticResource TopBarComboBoxToggleStyle}"/>

                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                           Placement="Bottom"
                           AllowsTransparency="True"
                           Focusable="False"
                           PopupAnimation="Fade">
                                <Border Background="#1B2028"
                                BorderBrush="#313845"
                                BorderThickness="1"
                                CornerRadius="12"
                                Margin="0,6,0,0"
                                MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer MaxHeight="260">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#465062"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#D90429"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="False">
                                <Setter Property="MinHeight" Value="40"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Border x:Name="MainShell"
        Background="#0D1017"
        BorderBrush="#2A3040"
        BorderThickness="1"
        CornerRadius="28">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="290"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- SIDEBAR -->
            <Border x:Name="SidebarBorder"
                    Grid.Column="0"
                    CornerRadius="14,0,0,14"
                    BorderThickness="0,0,1,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="18"
                                  ShadowDepth="4"
                                  Direction="0"
                                  Opacity="0.80"
                                  Color="#000000"/>
                </Border.Effect>

                <Grid Margin="22">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,6,0,28">
                        <Border Width="58"
                                Height="58"
                                CornerRadius="14"/>

                        <StackPanel Margin="14,4,0,0">
                            <TextBlock
                                       Foreground="White"
                                       FontSize="16"
                                       FontWeight="SemiBold"><Run Language="it-it" Text="UAE Engineering"/></TextBlock>
                            <TextBlock Text="Project Tracker"
                                       Foreground="{StaticResource SidebarMutedTextBrush}"
                                       FontSize="15"
                                       FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Row="1">
                        <Button x:Name="btnDashboard"
                                Style="{StaticResource SidebarButtonStyle}"
                                Tag="Active"
                                Click="btnDashboard_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="26"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="⌂"
                                           Style="{StaticResource SidebarIconTextStyle}"/>
                                <TextBlock Grid.Column="1"
                                           Text="Dashboard"
                                           Style="{StaticResource SidebarIconTextStyle}"
                                           Margin="10,0,0,0"
                                           FontWeight="SemiBold"/>
                            </Grid>
                        </Button>

                        <Button x:Name="btnDocuments"
                                Style="{StaticResource SidebarButtonStyle}"
                                Click="btnDocuments_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="26"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="≣"
                                           Style="{StaticResource SidebarIconTextStyle}"/>
                                <TextBlock Grid.Column="1"
                                           Text="Documents"
                                           Style="{StaticResource SidebarIconTextStyle}"
                                           Margin="10,0,0,0"/>
                            </Grid>
                        </Button>

                        <Button x:Name="btnProjects"
                                Style="{StaticResource SidebarButtonStyle}"
                                Click="btnProjects_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="26"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="▣"
                                           Style="{StaticResource SidebarIconTextStyle}"/>
                                <TextBlock Grid.Column="1"
                                           Text="Projects"
                                           Style="{StaticResource SidebarIconTextStyle}"
                                           Margin="10,0,0,0"/>
                            </Grid>
                        </Button>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Margin="0,24,0,6"/>
                </Grid>
            </Border>

            <!-- RIGHT SIDE -->
            <Grid Grid.Column="1" Background="{StaticResource BodyBackgroundBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="78"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                    Background="{StaticResource TopbarBrush}"
                    BorderBrush="#252B34"
                    BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TopBar_MouseLeftButtonDown">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="18"
                                          ShadowDepth="4"
                                          Direction="0"
                                          Opacity="0.45"
                                          Color="#000000"/>
                    </Border.Effect>
                    <Grid Margin="24,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="700"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Grid VerticalAlignment="Center" Margin="0,2,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="350"/>
                                <ColumnDefinition Width="18"/>
                                <ColumnDefinition Width="250"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Project"
                   Style="{StaticResource TopBarLabelStyle}"/>
                                <ComboBox x:Name="cmbProjects"
                  Width="330"
                  Style="{StaticResource TopBarComboBoxStyle}"
                  SelectionChanged="cmbProjects_SelectionChanged"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Environment"
                   Style="{StaticResource TopBarLabelStyle}"/>
                                <ComboBox x:Name="cmbEnvironments"
                  Width="230"
                  Style="{StaticResource TopBarComboBoxStyle}"
                  SelectionChanged="cmbEnvironments_SelectionChanged"/>
                            </StackPanel>
                        </Grid>

                        <StackPanel Grid.Column="2"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
                            <Button x:Name="btnMinimize"
                    Content="—"
                    Style="{StaticResource WindowCommandButtonStyle}"
                    Click="btnMinimize_Click"/>

                            <Button x:Name="btnMaximize"
                    Content="□"
                    Style="{StaticResource WindowCommandButtonStyle}"
                    Click="btnMaximize_Click"/>

                            <Button x:Name="btnClose"
                    Content="✕"
                    Style="{StaticResource CloseButtonStyle}"
                    Click="btnClose_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Frame x:Name="MainFrame"
                       Grid.Row="1"
                       NavigationUIVisibility="Hidden"
                       Background="{StaticResource BodyBackgroundBrush}"/>
            </Grid>
        </Grid>
    </Border>
</Window>